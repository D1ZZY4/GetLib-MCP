/**
 * Shared Select menu chrome. The popover ships with zero padding, so option
 * backgrounds butt against its rounded corners and the corners look broken.
 * One spacing step of inset plus the default rounded items keeps every
 * dropdown smooth. Apply to every Select.Popover.
 */
export const SELECT_MENU_POPOVER_CLASS = "p-1";
